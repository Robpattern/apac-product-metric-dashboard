export const metadata = {
  title: "APAC Technology — projects to metrics",
  description: "Where APAC engineering effort sits against Pattern's top metrics.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
